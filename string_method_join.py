"""
str.join(iterable) is a stirng method. Only string can call this method. The
arguments musb be iterable(eg. list, tuple), the elements in the iterable
also must be string.
"""

list = ["alike", "book", "Cisco"]

print("\n".join(list))
